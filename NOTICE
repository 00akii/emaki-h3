絵巻H3 / Emaki H3 — NOTICE

The MIT License in LICENSE covers **this application only**.

Not included, and not covered by it:

- **MiniMax-H3 model weights** — governed by the MiniMax H3 Community License Agreement.
  Note its "Applicable Territory": worldwide **excluding the EU, the UK, South Korea and the United States**.
  https://huggingface.co/MiniMaxAI/MiniMax-H3/blob/main/LICENSE
- **ComfyUI**, **LM Studio**, **Eagle** — each under its own license.

You obtain and run these yourself. See "必要なもの" / "What you need" in README.

Included, and covered by its own MIT licence:

- **`workflows/emaki_h3_ref2va.json`** — derived from ComfyUI's official workflow template
  `video_minimax_h3_r2v.json` (the `comfyui-workflow-templates` package, MIT).
  Only the sampler settings were changed: `res_multistep`/`simple`/20 steps → `euler`/`normal`/6 steps,
  to match the Turbo LoRA this app assumes. Everything else is unmodified.

---

絵巻H3 / Emaki H3 — 注記

`LICENSE` の MIT ライセンスが対象にするのは、**このアプリ本体だけ**です。

同梱しておらず、MIT の対象でもないもの:

- **MiniMax-H3 の重み** — MiniMax H3 Community License Agreement に従います。
  利用可能地域は「全世界から **EU・英国・韓国・米国を除く**」と定められています。
- **ComfyUI**、**LM Studio**、**Eagle** — それぞれ各自のライセンス。

これらは使う方がご自身で用意します。README の「必要なもの」を見てください。

同梱しており、それ自身の MIT ライセンスに従うもの:

- **`workflows/emaki_h3_ref2va.json`** — ComfyUI 公式のワークフローテンプレート
  `video_minimax_h3_r2v.json`（`comfyui-workflow-templates` パッケージ・MIT）から作りました。
  変えたのはサンプラーの設定だけです（`res_multistep`/`simple`/20 step →
  `euler`/`normal`/6 step。このアプリが前提にしている Turbo LoRA に合わせるため）。ほかは無改変です。
