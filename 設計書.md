# 絵巻H3 — 設計書（認識合わせ用）

2026-08-23。ローカル LLM で MiniMax-H3 の構造化プロンプトを書き、ComfyUI で生成し、結果を確認するまでを1つの画面でやる Web アプリ。

**この文書は「作る前に合意する」ためのもの。** 下の「決めたこと」は認識合わせで確定した。「未決」の項目は最初のバージョンでは推奨案で進め、動いてから直す。

---

## 1. 決めたこと（認識合わせ 2026-08-23）

| 項目 | 決定 |
|---|---|
| 形 | ローカル Web アプリ（ブラウザで開く） |
| 技術 | Python + FastAPI + 素の HTML/JS。ビルド不要 |
| 範囲（v1） | ブリーフ → プロンプト → **人が確認** → 生成 → 確認 |
| 人の確認点 | プロンプト生成後。生成前に読んで直せる |
| 生成 | 低解像度プレビュー（608×352・約3分） → 本番（1344×768・約18分）の二段 |
| プロジェクト | 作品単位で JSON 保存（スタイル宣言・キャラ・参照素材・既定値） |
| ワークフロー | 既存 `video_minimax_h3_i2v.json` を読んで必要箇所だけ書き換え、`/prompt` に投げる |
| 置き場所 | ComfyUI の `output\MiniMax-H3\app\`（公開時はここだけを切り出す） |
| 切り抜き | **v1 に含める**（2026-08-23 変更）。参照素材の「＋ 切り抜く」から SAM3 画面を開く。部品は `check_cut.py`（vendor に同梱）と、SAM3 セッションの `sam3_run.py` / `sam3_pick.py`（単体 CLI。開発機のパスを持つので vendor には入れない） |
| 公開 | **する**（2026-08-23 決定）。パス・モデル・ComfyUI の場所は `config.json` に外出しし、部品はアプリに同梱する |

---

## 2. 全体の流れ（1ショットを作る）

```
[プロジェクト選択]
      │  スタイル宣言 / キャラ定義 / 参照画像 / 既定値 が読み込まれる
      ▼
[参照素材]  切り抜き済みを選ぶ  ／  生画像 → [切り抜く] SAM3 検出 → 誰を残すか → プレビュー → 保存
      │  生画像のままでもプレビュー生成は通る。本番では背景が漏れるので本番前に切り抜きを求める
      ▼
[ブリーフ入力]  モードA: 1行 / モードB: 4欄 / モードC: 全キー
      │
      ▼
[プロンプト生成]  h3gen.py（LM Studio、自動修復つき、15〜30秒。ブリーフの長さで変わる）
      │  リンター結果（ERROR/WARN）を併記
      ▼
[プロンプト確認]  ← 人がここで読む・直す・やり直す  ★唯一の必須確認点
      │
      ├─▶ [プレビュー生成]  608×352、約3分。コンタクトシート表示
      │         │
      │         └─▶ 気に入らなければブリーフかプロンプトへ戻る
      │
      └─▶ [本番生成]  1344×768、約18分。完了後コンタクトシート + 動画
                │
                ▼
         [保存]  プロンプト.txt / アーカイブ .txt+.md / 動画パスを記録
```

**LM Studio と ComfyUI は GPU を同時には使えない**（27B が 19GB、H3 が 17GB 以上）。
アプリが裏で切り替える: プロンプト生成時に LM Studio をロード、生成投入前に `lms unload --all`。
**これが v1 で一番面倒な部分。** 切り替えに 20〜60 秒かかるので進捗表示が要る。

---

## 3. 画面（1ページ、左右2ペイン）

```
┌─────────────────────────────────────────────────────────────────┐
│ 絵巻H3      [プロジェクト: 夏の終わりの窓 ▼]  [GPU: LM Studio ● / ComfyUI ○] │
├────────────────────────┬────────────────────────────────────────┤
│ ■ ブリーフ              │ ■ プロンプト                            │
│  (A) おまかせ           │  subject_definitions:                   │
│  (B) 推奨   ← 既定      │  <Subject 1> is ...                     │
│  (C) 詳細               │  ...                                    │
│                        │  （編集可能なテキストエリア）             │
│  場所と時間 [________]  │                                         │
│  動き      [________]   │  [再検査]  ERROR 0 / WARN 1             │
│    → 立つ → 振り向く    │    WARN D9 素材名が2種類                 │
│  開始の構図 [チップ][__]│                                         │
│    ● そのまま通す       │                                         │
│  カメラ(終端)[選択▼][_] │  [プレビュー生成 608×352 ~7分]           │
│  画面内の文字 [______]  │  [本番生成 1344×768 ~16分]              │
│    ● 出ます             │                                         │
│  セリフ    [________]   │                                         │
│                        │                                         │
│  尺 [8▼] 比率 [16:9▼]  │                                         │
│  参照画像 [✓front][✓back]│                                        │
│  参照動画 [walk.mp4 ▼]  │ ■ 結果                                  │
│                        │  [コンタクトシート 3×3]                  │
│  [プロンプト生成]       │  [動画プレイヤー]                        │
│                        │  seed 1 / 192f / 8.00s / 11.1Mbps        │
│ ■ 進捗                  │  [この結果を採用] [もう一度] [seed変更]   │
│  LM Studio ロード中 ▓▓░ │                                         │
└────────────────────────┴────────────────────────────────────────┘
```

- **モードB が既定。** 6欄（場所と時間／動き／開始の構図／カメラ〔終端〕／画面内の文字／セリフ）。当初は4欄で、検証を経て2欄が増えた:
  - **開始の構図**（軸1）と**カメラ＝終端**（軸2）は別物。カメラ選択肢10種は全部が終端構図だったため、
    「どこから始まるか」の軸を新設した（`FRAMING_PRESETS` 15件・効いた語だけ）。**選ぶと `check_framing` が
    LLM を使わず 3分岐**（そのまま通す／本文を画角内に絞る修正／成立しにくい警告）し、判定を欄の下に即時表示する
  - **画面内の文字**は `TEXT:` として `SHOT:` とは別行で渡す。寄りの画角で場所欄が背景ヒントに降格されても巻き込まれない。
    入力時に `textcheck` が JIS 水準を機械判定して警告（実測: 常用の範囲は 9/9、稀少字は別の実在漢字になる）
  - **効かないものは選択肢に置かない。** 選んでも変わらないものを並べるのは「選んだのに効かない」という UX の毒になる
- モードA は1行入力 → 「こう解釈しました」として**モードB の欄に展開して見せてから**生成に進む（誤入力のガード）。**`画面内の文字` は未対応**
- モードC は全キー（`MODE/DURATION/RATIO/STYLE/REFS/SHOT/FRAMING/CAMERA/TEXT/DIALOGUE/SOUND/MUSIC`）
- プロンプト欄は**編集可能**。手で直したら「再検査」でリンターを通せる
- 進捗は画面が `GET /api/jobs/{id}` をポーリングして出す（LM Studio ロード → 生成 → 検査 → ComfyUI 切替 → サンプリング step/6 → デコード）。**設計時は WebSocket で流す案だったが、ポーリングで足りた**（ComfyUI 側の WS はサーバー内部で受けている）

---

## 4. プロジェクト JSON

```
app/projects/<作品名>/project.json
```

```json
{
  "name": "夏の終わりの窓",
  "style": "2D-animated, hand-drawn 2D anime with soft thin inked outlines, flat two-tone cel shading, a muted palette, and watercolor-textured painted backgrounds",
  "subjects": [
    {
      "label": "Subject 1",
      "description": "the high-school girl ... with dark brown chin-length hair carrying a soft cyan-tinted highlight band ...",
      "images": ["h3ref_A_profile_cut.png", "h3ref_C_front1_cut.png"],
      "motion_video": "A_static_medium_wide_shot_of_a.mp4",
      "motion_description": "a full-body clip of a woman standing and facing forward, turning into a side profile, ..."
    }
  ],
  "defaults": { "duration": 8, "ratio": "16:9", "music": "N/A" },
  "comfy": {
    "workflow": "video_minimax_h3_i2v.json",
    "preview":  { "megapixels": 0.21, "steps": 6 },
    "final":    { "megapixels": 0.98, "steps": 6 },
    "seed": 1
  },
  "shots": [
    { "id": "S14", "brief": {...}, "prompt_file": "...", "videos": ["MiniMax_H3_00058.mp4"], "note": "..." }
  ]
}
```

- `subjects[].description` は **VLM の下書き（`vlm_test.py`）を人が直したもの**を保存する。以後ショットごとに書かない
- `shots[]` に生成履歴を積む。**連続性検証（5秒連結）はこの単位で前クリップの最終フレームを参照する**
- 参照画像は `<ComfyUI>\input\`（`config.comfy_input_dir`）のファイル名で持つ（ComfyUI がそこしか読まないため）

---

## 5. バックエンド API

**実装の一覧（2026-08-24 現在・51 本）。** 設計時の案ではなく `server.py` から起こしたもの。

```
GET  /                          画面（app.js / style.css に更新時刻を付けて返す＝キャッシュ対策）
GET  /api/config                設定（実値）           PUT  /api/config
GET  /api/preflight             ノード・重みの存在確認（GPU 不要）

GET  /api/projects              一覧                   POST /api/projects            新規
GET  /api/projects/{name}       読み込み               PUT  /api/projects/{name}     保存
GET  /api/assets/images         input\ の *_cut.png    GET  /api/assets/videos       参照動画
GET  /api/assets/raw            切り抜き前の元イラスト
GET  /api/file/input|raw|output/{name}                 画像・動画の実体

GET  /api/models                一覧（実測ラベルつき） POST /api/models/select
POST /api/models/load           載せる（VRAM 検算つき）POST /api/models/unload
POST /api/models/fix-context    LM Studio の既定 ctx を直す（バックアップあり）
GET  /api/gpu                   どちらが GPU を持っているか＋LLM が載りきっているか
POST /api/gpu/free-comfy        ComfyUI の常駐モデルを降ろす

POST /api/brief/build           欄 → ブリーフ文字列（LLM を使わない）
POST /api/brief/steps           「動き」欄の区切り解釈（同上）
POST /api/brief/framing-check   開始の構図 × 終端 の判定 pass|fix|warn（同上）
POST /api/brief/text-check      画面内の文字の点検（JIS 水準・長さ。同上）
POST /api/brief/expand          モードA の1行 → 欄に展開（LM Studio。**未検証**）
GET  /api/camera/presets        {presets（終端10件）, framing（開始15件）}

POST /api/prompt/generate       ブリーフ → プロンプト（自動修復つき）→ {prompt, lint, framing_check, text_check}
POST /api/prompt/lint           手で直したプロンプトを再検査
POST /api/prompt/write          プロンプト.txt に書き出し（アーカイブも）
POST /api/prompt/notes          解説 .md（**未検証**）

POST /api/generate              GPU 切替 → API グラフ組み立て → /prompt 投入 → job_id
GET  /api/jobs                  一覧                   GET  /api/jobs/{id}           状態（進捗・ログ）
GET  /api/jobs/{id}/contact     コンタクトシート       POST /api/jobs/{id}/cancel    中止
GET  /api/video?rel=            出力動画               GET  /api/workflow            ワークフローから読んだ固定値
GET  /api/history               アーカイブ一覧         GET  /api/history/{name}      1件
POST /api/shots/archive         採用 → プロンプト.txt 上書き + アーカイブ + project.shots 追記

GET  /api/eagle/info            接続確認               GET  /api/eagle/folders       フォルダ一覧
POST /api/eagle/send            動画（と任意でシート）を送る

GET  /api/cut/available         SAM3 が使えるか        POST /api/cut/sweep           threshold を6点振る
POST /api/cut/detect            SAM3 を1回だけ回す → 検出ごとの原寸マスクを cutcache へ
POST /api/cut/preview           選んだ人物だけ合成（ローカル numpy。GPU を使わない）
POST /api/cut/save              input\ に保存 → check_cut → 参照一覧に追加
POST /api/cut/check             保存済み画像の再検査   GET  /api/cut/file/{session}/{name}
```

**進捗は WebSocket ではなくポーリング**（`GET /api/jobs/{id}` を画面が叩く）。ComfyUI 側の WS はサーバー内部で使う。

**既存部品をそのまま import する。** コピーしない。

| 部品 | 場所 | 用途 |
|---|---|---|
| `h3gen.generate()` | `../ローカルLLM/h3gen.py` | プロンプト生成＋自動修復 |
| `h3lint.check()` | `../ローカルLLM/h3lint.py` | 検査 |
| `system_h3.txt` | `../ローカルLLM/` | システムプロンプト（v11） |
| `system_h3_notes.txt` | `../ローカルLLM/` | 解説 .md 生成 |
| `vlm_test.ask()` | `../ローカルLLM/vlm_test.py` | キャラ表から記述の下書き |
| `check_cut.analyze()` | `../SAM3/check_cut.py` | 参照画像の事前検査（選んだ画像が単色背景か） |

---

## 6. ComfyUI ワークフローの扱い（重要な設計判断）

現在の `video_minimax_h3_i2v.json` は **参照画像の入口が SAM3 サブグラフ経由**になっている
（`LoadImage 6216 → サブグラフ f87cfe69 (SAM3切り抜き) → MiniMaxH3ReferenceToVideo.ref_image_0`）。

**アプリでは SAM3 サブグラフを通さない。** 理由:

1. 切り抜きは SAM3 セッション側で済ませ、`_cut.png` を使う前提（役割分担）
2. 生成のたびに SAM3 が走ると VRAM と時間を食う
3. `person:30,...` の既定テキストは群像で全員拾うので、入口で勝手に切るのは危ない

したがってアプリは **API 形式の prompt JSON を自前で組み立てる**（`sam3_run.py` と同じやり方）。
`graphToPrompt()` 相当を既存 JSON から作り、次だけ差し替える:

| 何を | どのノード | 値 |
|---|---|---|
| プロンプト本文 | `MiniMaxH3ReferenceToVideo.prompt` | 直接文字列を入れる（`TextFileLoaderMC` 経由をやめる） |
| 参照画像 | `ref_images.ref_image_N` | `LoadImage(_cut.png)` を直結 |
| 参照動画 | `ref_videos.ref_video_0` | `VHS_LoadVideo`（608×352 / frame_load_cap 97 / skip 48 の検証済み値） |
| 尺 | `length` | `frames_for(duration)`（h3lint の関数、`n%17==5` 切り上げ） |
| 解像度 | `width/height` | preview 608×352 / final 1344×768 |
| seed | `RandomNoise.noise_seed` | プロジェクト既定 or UI 指定 |

サンプラー系（euler / normal / 6 step / FP8 / Turbo LoRA）は検証済み値を固定で持つ。

**プロンプト.txt の運用は維持する。** アプリは採用時に `プロンプト.txt` を上書きしてアーカイブも書く。
既存の手動ワークフローと両立させる（アプリが壊れても今までどおり回せる）。

---

## 7. GPU の切り替え

```
状態:  LLM（LM Studio 27B 19GB） | COMFY（H3 17GB+） | FREE
遷移:  generate_prompt → LLM が要る → ComfyUI が走っていたら待つ/拒否 → lms load
       generate_video  → COMFY が要る → lms unload --all → ComfyUI 起動確認 → /prompt
```

- ComfyUI は**アプリが起動しない**（別プロセスとして立ち上がっている前提。`/system_stats` で生存確認）。起動していなければ画面に「ComfyUI を起動してください」と出す
- LM Studio はアプリが `lms load/unload` を叩く（`--context-length 16384 --parallel 1 --speculative-draft-mtp`）
- ComfyUI のキューに他のジョブがあるときは投入しない（他セッションの生成を壊さない）

---

## 8. 生成結果の確認

ffmpeg で自動生成して画面に出す（S14/S15 で手でやった手順をコード化）:

- **コンタクトシート** 3×3（フレーム 0/24/48/72/96/120/144/168/191）
- **メタ**: フレーム数・実尺・bit_rate（11Mbps 前後が正常、極端に低いと破損疑い）
- **音量** mean/max（-45dB 前後が正常。無音なら異常）
- **フレーム間差分**（ちらつきの目安。前回の数値と並べる）

採用/不採用は人が決める。自動判定はしない。

---

## 9. ファイル構成

```
app/
  server.py            FastAPI 本体
  h3studio/
    __init__.py
    brief.py           モード A/B/C → system_h3 に渡すブリーフ文字列
    promptgen.py       h3gen/h3lint のラッパ
    comfy.py           ワークフロー組み立て・/prompt 投入・/history 監視・/view 取得
    gpu.py             lms load/unload・ComfyUI 生存確認・排他
    inspect.py         ffmpeg によるコンタクトシート/メタ/音量/差分
    cut.py             SAM3 切り抜き（sam3_run / sam3_pick / check_cut のラッパ）
    config.py          config.json の読み込み・既定値・存在チェック
    project.py         project.json の読み書き
  vendor/              同梱する部品（公開時に ../ローカルLLM への依存を切る）
    h3gen.py  h3lint.py  system_h3.txt  system_h3_notes.txt  vlm.py
    sam3_run.py  sam3_pick.py  check_cut.py
  config.example.json  公開用の雛形（パス・モデル・ComfyUI の場所）
  config.json          ローカルの実値（git 管理外）
  static/
    index.html
    app.js
    style.css
  projects/
    <作品名>/project.json
  README.md
  設計書.md            ← この文書
```

依存: `fastapi` `uvicorn`（システム Python に導入済み）、`ffmpeg`（PATH にあり）。

起動:
```bash
cd <このフォルダ>
python server.py        # → http://127.0.0.1:8765
```

---

## 9a. 実装中に踏んだ罠（2026-08-23）

### LM Studio の「モデル既定設定」が `lms load --context-length` より優先される

`~/.lmstudio/.internal/user-concrete-model-default-config/<publisher>/<repo>/<file>.gguf.json` に
`llm.load.contextLength` が保存されていると（GUI でモデル設定を触ると出来る）、**`lms load --context-length 16384` を渡してもその値は無視され、ファイルの値が使われる。**

実測: ファイルに 98304 が入っていた → `lms ps` は 58624（VRAM に合わせて自動で切り詰めた値）→ VRAM 23.3GB → **プロンプト生成が 16秒 → 215秒**。README の「既定ロードの罠」と同じ症状だが、原因は CLI フラグではなくこのファイル。

アプリ側の対処（`llm.ensure_loaded`）:
1. load 後に `lms ps --json` の `contextLength` を読み、要求値と違えば `warning` を返す（画面に出す）
2. `pinned_context()` でそのファイルと値を特定して示す
3. `config.lmstudio_load.auto_fix_pinned_context: true` のときだけ、バックアップを取ってファイルを書き換え → 載せ直す（既定は off。人の設定を黙って変えない）

**公開時の注意:** 使う人の LM Studio にも同じファイルがあり得る。初回ロード後に実効 ctx を必ず表示する。

### 2本目以降の生成が 10 倍以上遅くなる（ComfyUI 0.33 の動的 VRAM ローダー）— 段4で踏んだ

実測（`user/comfyui_8189.log` の `[step]` 行、本体パッチのログ）:

| 条件 | サンプラー開始時の空き VRAM | step 1 | step 2〜6 |
|---|---|---|---|
| 1本目。参照エンコードの VAE(5GB) が残ったまま UNET を載せる | 17.4 GB | 185 s | 9.4 s/step |
| 2本目。前回の UNET+VAE が残ったまま（`0 models unloaded`） | 0.5 GB | 8分で 15/50 ブロック → 中止 | — |
| `/free` → H3 ノード再実行 → **Purge 直後に UNET** | 17.7 GB | **15 s** | 9.2 s/step |
| `/free`（モデルのみ）→ H3 はキャッシュ → UNET 直行 | **20.4 GB** | **606 s**、step 2 も 10分以上 → 中止 | — |
| `/free`（モデル＋キャッシュ）→ H3 再実行 → Purge → UNET | — | 188 s | 9.4 s/step |

空き VRAM が UNET（19983MB Staged）以上あると動的ローダーが**全部載せる**→活性化メモリ（約3GB）の分だけ毎ブロック重みを追い出し・読み直し。空きが少し足りないと一部をオフロードしたまま回って速い。**空きが多いほど遅い**という逆転。VAE デコードも同じ（UNET 常駐のまま VAE を載せると 6 分、Purge して載せ直すと 11 秒）。

> **⚠ この規則は 2026-08-23 の条件での実測であり、いまのアプリ設定では予測に使えない。必ず下の「追記2」まで読むこと。**
> 同じ空き VRAM から 20 倍違う結果が出る組が2つ見つかっている。

#### 追記（2026-08-25）— **この罠は「遅い」で済まず、完了も中止もできない状態に落ちる**

`vram_mode: auto` の**遷移1本目**（share の直後に resident へ移る本）で実測:

```
06:18:06 block  1/50  free 6957 MB
06:19:57 block 39/50  free    0 MB
06:21:07 block 40/50  free    0 MB   (+70秒)
06:22:48 block 41/50  free    0 MB   (+101秒)
06:25:17 block 42/50  free    0 MB   (+149秒)   ← 間隔が単調に悪化
06:28:08 Global interrupt 受理（200 が返る）
06:35    block 43 まだ出ず（7分以上）→ プロセスを kill して回復
```
GPU は util 100% だが**実消費 111W**（リミット 360W）。**演算していない。転送で溺れている。**

**⚠ `/interrupt` は効かない。** ComfyUI は割り込みを**ブロック境界でしか見ない**ので、1ブロックが終わらない状態では止まらない。
**HTTP 200 が返っても止まっていない。この状態からの回復手段はプロセスの kill だけ。**

**同じ位置（遷移1本目）の本を条件別に並べると、`--reserve-vram` の有無で結果が割れている:**

| 電力 | `--reserve-vram` | 結果 |
|---:|---|---|
| 50% | 無し | 完走 235.2秒（step1 177.3秒） |
| 80% | **有り** | 完走 182.3秒（**step1 56.8秒**） |
| 80% | 無し | **発散・中止不能** |

**n=1 ずつで、電力とフラグが交絡している。**「フラグが効く」と読みたくなるが、**まだ言えない**。
確定させるには同一条件で複数本が要る（2026-08-25 時点で測定中）。
**確実に言えるのは「フラグ無しの遷移1本目は、完走することもあれば発散することもある＝予測できない」まで。**

#### ⚠ 追記2（2026-08-25 夕）— **上の「空きが多いほど遅い」は、いまのアプリ設定では予測に使えない**

`block 1/50` 時点の空き VRAM を全本で拾ったところ、**同じ空き VRAM から違う結果が出る組が2つ**見つかった:

| 組 | `block 1/50` の空き VRAM | 結果 |
|---|---:|---|
| 511 対 **612** | どちらも **20638 MB** | step1 **10.28秒** 対 **212.93秒**（**20.7倍**） |
| 611 対 613 | どちらも **6957 MB** | **発散・中止不能** 対 詰まるが進行 |

全本:

| 本 | mode | block1 free | step1 | encode | 合計 |
|---|---|---:|---:|---:|---:|
| 511 | share | 20638 MB | 10.28秒 | 88.7秒 | 141.6秒 |
| 512 | share | 20468 MB | 6.90秒 | 85.0秒 | 132.8秒 |
| 513 | share | 20564 MB | 6.45秒 | 83.0秒 | 127.9秒 |
| 514 | share | 20564 MB | 6.29秒 | 83.0秒 | 126.2秒 |
| **612** | share | **20638 MB** | **212.93秒** | 79.5秒 | **336.0秒** |
| 512 | resident | 0 MB | — | — | 182.3秒 |
| 611 | resident | 6957 MB | 発散 | — | kill |

**§9a の規則は両方向で外れている:**
- 「空きが UNET(19983MB) 以上なら遅い」→ **20.5GB 空きの5本中4本が 6〜10秒で速い**
- 「空きが少し足りなければ速い」→ **6957MB 空きの2本はどちらも詰まった**

**上の表（2026-08-23）を消していないのは、あれが別の条件での実測だから**（解像度も経路も違う。`/free`＋Purge を常に通す今の設定とは前提が違う）。
**ただし、いまのアプリ設定の下では、空き VRAM から結果を予測してはいけない。**

**現時点で言えること:**
- **step1 の詰まりは `vram_mode` に依らず起きる**（share の 612 で発生）
- **同一条件から違う結果が出る＝決定的でない。** 何が分けているかは**分かっていない**
- **頻度は測っていない。**「n 本中 m 本」は観測であって発生率の推定ではない
- したがって**平均値でモードを比較しても意味が薄い。**見るべきは「詰まりが出るかどうか」

#### 追記3（2026-08-25）— **`--reserve-vram 3` は詰まりを防がない**

`--reserve-vram 3` を付けて ComfyUI を起動した状態で、**share の 3本中1本が詰まった**（713・**step1 494.08秒**・合計 620秒）。
**これは記録した中で最悪の share の詰まり**（フラグ無しの最悪は 612 の step1 212.93秒）。

| | step1 | 合計 |
|---|---:|---:|
| フラグ**有り** 711 / 712 / 713 | 12.39 / **47.83** / **494.08秒** | 139.6 / 175.3 / **620.0秒** |
| フラグ**無し** 511-514 / 612 | 6.29〜10.28 / **212.93秒** | 126.2〜141.6 / **336.0秒** |

**⚠ 「フラグを付けると遅くなる」とは書けない**（n=3 対 n=5）。**言えるのは「フラグを付けた状態でも詰まりは出る」まで。**
これは**1本の反例で決まる主張**なので、この点だけは確定している。

**⚠ フラグが内部で honor されているかは、外から観測できない。**
`[step]` ログの「空き」は `torch.cuda.mem_get_info()`（`comfy/utils.py:1236`）＝**ドライバの実測値**で、
`--reserve-vram` が変えるのは ComfyUI 内部のロード判断用の数値。`/system_stats` の `vram_free` もフラグの有無で変わらない。
**したがって得られるのは振る舞いの証拠だけ。**

**未検証で残っているもの: フラグが「発散（611 型・`/interrupt` が効かず kill 以外に回復手段が無い状態）」を防ぐか。**
遅くなっても完走するなら、公開するアプリにとっては意味のある差になる。

#### 追記（同日夕、カメラ検証61本の実測を受けて）— 常駐そのものは悪くない

同じ日にカメラ検証セッションが回した61本は、**`/free` も Purge も無い15ノード構成**で **3分40秒/本**を維持した。
UNET が載りっぱなしで**再読込が一度も起きなかった**ため。上の表と矛盾しない:

**遅いのは「常駐」ではなく「全部解放 → 空いた GPU へ再読込」。** 空きが多いほど動的ローダーが全部載せようとして自滅する。**（⚠ この説明も 2026-08-23 時点のもの。§9a 追記2 のとおり、空き VRAM は結果を予測しない。「常駐のほうが速い」という結論そのものは 2026-08-25 の実測でも支持されている — 連投中 45/49 秒 対 1本ずつ 126〜142 秒。）**
解放しなければそもそも載せ直しが発生しない。

そこで `config.gen.vram_mode` を入れた（`comfy.decide_vram_mode()`）:

| 値 | 動作 | 使いどころ |
|---|---|---|
| `share`（**既定・検証済み**） | `/free` + purge ノード | LM Studio と GPU を取り合う通常運用 |
| `resident` | 何もしない | LLM を挟まない連投 |
| `auto` | 直前のジョブが**同じモデル構成で成功**し、かつ今回 **LM Studio を降ろしていない**ときだけ `resident` | seed 違いの再生成 |

**⚠ `/free` だけを条件分岐にしても効かない。** `purge2` は `purge_models=True` なので、
`/free` を止めても**各ジョブの終わりに UNET が降ろされ、常駐にならない**。解放と purge は必ず組で切り替える。

中止・エラーの後は VRAM に何が載っているか分からないため、`_LAST_RUN` を捨てて次を必ず `share` に倒す。
既定を `share` のままにしてあるのは、**resident / auto をまだ実測していない**から（引き継ぎ書 §7）。

アプリの対処（`config.gen`）: `free_vram_before`（投入前 `/free`）/ `free_cache_before`（実行キャッシュも消して H3 を毎回走らせ、Purge→UNET の順を確実にする。CPU テキストエンコード ~150-210 s が毎回かかる代償）/ `purge_node`（サンプラー前と VAE デコード前に `LayerUtility: PurgeVRAM V2`。無い環境では `/free` のみ）。step 1 が **300 秒**を超えたら進捗ログに警告（`comfy.py` の実値。以前この文書は 240 秒と書いていた・2026-08-24 にメンテ担当の指摘で訂正）。

**⚠ 以前ここには「筋の良い対処は `--reserve-vram 3`」と書いてあったが、2026-08-25 の実測15本で撤回した（§9a 追記3）。**
付けて起動した状態でも**詰まりは出る**（step1 494秒）し、**完了も中止もできない状態にもなる**（連投中の本で発生）。
**アプリが画面に出していた推奨も削除済み。** `/api/gpu` の `reserve_vram` はフラグの有無を返すだけで、**効いているかは外から観測できない**。検証できたら `free_cache_before` を false に戻して seed 違いの再生成からテキストエンコードを省く。

付随: 同一入力の再投入は ComfyUI のキャッシュが返るだけ（`execution_cached` に出力ノードが含まれる）。アプリは検出して「再生成されていない」と表示。スラッシング中は ComfyUI の HTTP が固まる（`/interrupt` 応答 41 秒、停止まで数分）。

## 9b. 公開を前提にした設計（2026-08-23 追加）

**他の人にも使ってもらう。** そのために段1から次を守る。

### 環境依存は全部 `config.json` に出す

```json
{
  "comfy_url": "http://127.0.0.1:8189",
  "comfy_input_dir": "C:/ComfyUI/input",
  "comfy_output_dir": "C:/ComfyUI/output",
  "workflow_json": "C:/ComfyUI/user/default/workflows/video_minimax_h3_i2v.json",
  "prompt_txt": "C:/ComfyUI/output/MiniMax-H3/prompt.txt",
  "archive_dir": "C:/ComfyUI/output/MiniMax-H3/archive",
  "raw_dir": "C:/art/reference",
  "lmstudio_url": "http://localhost:1234",
  "lmstudio_model": "",
  "lms_cli": "lms",
  "ffmpeg": "ffmpeg",
  "port": 8765
}
```

起動時に存在チェックし、無いものは画面の「設定」に赤で出す。**コードにパスを書かない。**

### 「動作確認済み環境」を README に明記する

このアプリの数値（尺グリッド・安全上限10秒・euler+normal・6step・FP8・参照動画 608×352）は**全部 RTX 4090 24GB + ComfyUI 0.33.1 + 本体パッチ適用済み**の実測。他の環境では変わりうる。

- GPU 24GB 未満: LM Studio 27B と H3 の切替が前提なので、どちらかが載らない可能性がある。`モデル別の傾向.md` の小さいモデル（`qwen3.5-4b` 10/14）を案内する
- ステップ進行ログは本体パッチ（`comfyui-local-patches`）に依存している。**無くても動くが進捗が出ない**ので、パッチ無し環境ではポーリングに落とす

### モデルの違いを吸収する

- `lmstudio_model` を設定で差し替え可。画面に「検証済み: qwen3.6-27b（56/56）」「未検証」のラベルを出す
- `bench2.py` を「このモデルで自己診断」ボタンとして同梱する。使う人が自分の環境で合格率を測れる

### 依存を同梱する

`../ローカルLLM/` と `../SAM3/` を相対参照している部品は `app/vendor/` にコピーして同梱。**元の場所は開発用、配布物は vendor/ を正とする。** 更新時は `tools/sync_vendor.py` で同期。

### ライセンスと注意

- アプリ自体は MIT
- MiniMax-H3 の重みは **MiniMax H3 Community License**（US/EU/UK/韓国は個別許諾が要る）。README に明記し、重みは同梱しない
- ComfyUI・LM Studio・SAM3 はそれぞれのライセンスに従う。同梱しない
- 参照画像・生成物の権利はユーザーのもの。アプリは保存するだけ

### 公開までの段

| 段 | 内容 | 状態 |
|---|---|---|
| 7 | `config.json` 化の完了、パスのハードコードをゼロに | **済（2026-08-23 夜）** |
| 8 | `vendor/` 同梱、`../` 参照の除去 | **済** |
| 9 | 別の PC（または別フォルダ）で新規セットアップして動くことを確認 | **済** |
| 10 | README（英日）、スクリーンショット、既知の制限、動作確認済み環境 | **済**（スクリーンショットのみ未） |
| 11 | GitHub 公開 | 未着手 |

#### 段7〜10 で実際にやったこと（2026-08-23 夜）

- **段7** — 開発機のパスを持っていた唯一の場所 `config.py::write_example_from_local()` を削除。雛形は
  `tools/make_example_config.py` が `DEFAULTS` から起こす（手書きだと段4〜6bで増えたキーが必ず落ちる。実際 5 キー落ちていた）。
  `--check` でずれを非 0 で返すので、`DEFAULTS` を触ったら気づける。**`grep -rn "J:/"` は 0 件**
- **段8** — `tools/sync_vendor.py`。vendor 内の相対パス → 正本の絶対パスという 11 件の対応表がこのファイルにあり、
  「vendor に何が要るか」の唯一の定義になっている（`vlm.py` の正本が `vlm_test.py` という名前違いもここで吸収）。
  配布物には正本が無いので、その場合は何もせず 0 で終わる
- **段9** — **足りないノードで「原因の分かる止まり方」をさせる**のが主眼。`comfy.preflight()` が
  必須4／代替あり1／任意4 を `/object_info` で確認し、`model_files()` が重み5つの実在をファイル名で照合する
  （環境ごとに名前が違うので、これが無いと投入して初めて落ちる）。画面は「設定」の N・W 欄。
  代替: `VHS_VideoCombine` 無し → 本体 `CreateVideo`+`SaveVideo`、`DisplayAny` 無し → `PreviewAny`。
  `VHS_LoadVideo` 無しで参照動画を選んだ場合だけは**黙って落とさず、理由を出して止める**（プロンプトの `<Video N>` と食い違うため）。
  検査は `tools/validate_graph.py`（GPU 不要）で 3 構成を `/object_info` と突き合わせる。
  新規セットアップは `config.json` 無しの複製を別ポートで起動し、**赤い項目だけが出て他は全部 200**、
  最初の作品作成まで通ることを確認した
- **段10** — `README.md`（日本語・正本）/ `README.en.md`（英語・要約）/ `LICENSE`（MIT）/ `.gitignore`。
  ボタンの所要時間表示が設計時の見積（プレビュー約3分）のままだったので実測（約7分）に直した

### 段6b の設計判断（2026-08-23）

- **SAM3 を回すのは「検出」の1回だけ。** `individual_masks=True` で人物ごとの原寸マスクが1回で全部返るので、
  それを `cutcache/<session>/` に落とし、**プレビューも保存もローカルの numpy 合成で済ませる**。
  誰を残すか選び直すたびに GPU を触らない（切り替えが一瞬になる）
- **マスクの受け取りは `SaveImage` ではなく `PreviewImage`。** SaveImage だと ComfyUI の `output/` に
  マスク PNG が毎回溜まる。こちらで消すと、**同一パラメータの再検出でキャッシュされた history が
  消えたファイルを指して `/view` が 404** になる（実際に踏んだ）。PreviewImage は temp に書き、
  ComfyUI 本体が起動時に掃除するので溜まらず、キャッシュとも整合する
- **検出ゼロは 400（設定の問題）。** threshold が高すぎると `SAM3_Detect` が
  `index 0 is out of bounds ... size 0` で落ちる。ComfyUI の例外文を添えて「threshold を下げてください」と返す
- **クロップはローカルで。** ワークフローの `LayerUtility: CropByMask V2` に相当する処理（bbox＋余白）は
  numpy のスライスで足りるので、カスタムノード依存を増やさない
- **検出の直後にキューが空くまで待つ。** 待たないと次の検出が「他のジョブがある」と誤判定して 409 になる
- **番号を UI から追放する。** カードは絵・左から何番目・髪色・面積・score を出し、番号は小さく添えるだけ。
  順位は「画面の水平中心からの距離」で決まり人物とは無関係（SAM3 引き継ぎ §14〜15）

### Eagle 連携の設計判断（2026-08-23）

**ComfyUI の `SendToEagleVideo` ノードは使わず、Eagle の HTTP API（`localhost:41595`）を直接叩く。** 理由:

1. **ノード経由だと無音版と音付き版の2本が登録される。** `VHS_VideoCombine` の `Filenames` 出力に両方入るため。
   アプリは結果取得の時点で音付きの1本を特定しているので、それだけを送れる
2. **生成時にしか送れない。** 「あとから送る」「採用したものだけ送る」「過去のジョブを開き直して送る」ができない
3. **カスタムノード依存が増える。** 公開時に「これも入れてください」が1つ増える
4. **アプリが持っている情報を注釈に書ける。** ブリーフ・検査結果（bit_rate／音量／フレーム間差分）・作品名・ショットID・job id

実測（Eagle 4.0.0）: 既定でトークン不要。`/api/item/addFromPath` は**同じパスなら重複登録しない**（同じ item id が返る）ので、
再送のガードは要らない。フォルダは `/api/folder/list` を階層パス表記に平たくして選ばせる（手元は 272 フォルダ）。

### 「動き」欄の区切り（2026-08-23）

**「→」はキーボードに無くて面倒**というユーザーの指摘を受けて、区切りを自由にした。

調べた結果、**矢印に機能的な依存は元から無かった**: `brief.py` は動き欄を分割せずそのまま `SHOT:` に埋めていたし、
`system_h3.txt` にも `h3lint.py` にも矢印の指定・検査は無い。矢印はヘルプ文の例とモードA展開の指示だけに出ていた。

そこで `brief.split_steps()` を追加し、**打ちやすい記号を何でも受け付けて内部で `" → "` に正規化**する。
上から順に試して**最初に2つ以上へ分かれた区切りを採用**する方式:

| 優先 | 区切り | 備考 |
|---|---|---|
| 1 | `→` `⇒` `=>` `->` | 従来どおり。前後の空白は任意 |
| 2 | 改行 | **これを正規化しないと `SHOT:` 行が途中で折れてキー構造を壊す**（実測） |
| 3 | `、` `，` `,` `;` `；` | |
| 4 | 空白で囲まれた `-` `－` | 空白を要求するので `3-5歩` は割れない |
| 5 | 全角スペース | |
| 6 | 半角スペース | 日本語文では空白自体が区切りとして目立つ |

`ー`（長音）は**絶対に区切りにしない**（語尾を壊すため）。区切りが無ければ1段階として扱う。

**解釈結果は入力欄の直下にその場で出す**（`POST /api/brief/steps`、LLM は使わない）。
どの記号で割れたか・何段階になったかが見えるので、規則を覚えなくても間違いに気づける。

### 初期構図の軸（2026-08-23・検証結果）

カメラ選択肢は当初すべて**終端構図＝動きの着地点**で、「どこから始まるか」＝**初期構図の軸が無かった**。
その軸を作れるかを 24 本（段A 18 ＋ 追試A2 6）で測った。**結論: 作れる。ただし軸で性質が4通りに分かれる。**

| 軸 | 素の指定 | 本文を画角に合わせた追試 | 扱い |
|---|---|---|---|
| viewpoint（正面／横顔／背中） | 効く | — | 語を足すだけでよい |
| camera_height（目線／ハイ／ロー） | 効く | — | 語を足すだけでよい |
| shot_size（クローズアップ／ミディアム） | **効かない** | **効く** | 本文の制約とセット |
| dof（浅い被写界深度） | **効かない** | **効く** | 本文の制約とセット |
| lens（望遠圧縮／広角パース） | 効かない | 効かない | **選択肢に出さない** |
| tilt（ダッチアングル） | 弱い | 弱い | 出すなら期待値を下げる |
| 極端値（真俯瞰／地面すれすれ） | 効かない | — | 出さない |

**shot_size が単体で効かない理由**: 場面記述が画角に入らないもの（数 m 後方の椅子、奥の壁際の人物）を要求していると、
「顔のクローズアップ」と両立せず**本文が勝つ**。指定は黙って無視される。
画角に入るものだけを描写して測り直すと `close_up` と `shallow` が通った。

**効いた書き方は R4 と同じ原理**（タグではなく平叙文）:
`The frame holds her face and the tops of her shoulders.` /
`everything behind her dissolves into soft, unreadable out-of-focus shapes`。
つまり **R4「カメラを動かすのは終端構図の記述であって振幅タグではない」が、初期構図と被写界深度にも及ぶ**。

**設計上の注意（実装前に読む）:**
- **R8 と衝突しない書き方にする。** このモデルに negative prompt は無く「描くな」は書けない。
  制約は「画角に入るものだけを描く」という**肯定形**で表現する
- **R9 と衝突する。** R9 は参照画像の背景漏れを新しい場所の具体物で押し返せと指示するが、これは寄りの画角と矛盾する。
  寄りのときは R9 の適用範囲を画角内に狭める
- 検証は t2va（参照なし）で行った。**ref2va では参照画像自体が構図を引っ張る可能性があり未確認**
  → 同日の段B・C（ref2va 33本）で**引っ張られないことを確認**。ジャンル差もなし（`../カメラ検証/結論.md`）

### 画面内の文字（TEXT）の設計判断（2026-08-23 夜）

**問い**: 「手に持った看板に『こんにちは』」を出せるか。出せるなら欄を作るべきか。

**測ってから作った。** 公式 §4.5（画面に写る文字は英語のダブルクォートで囲み原文のまま）で構文は元から通るが、
**H3 が仮名を読める形で描くかは誰も測っていなかった**。効かない選択肢を出すのは UX の毒（カメラ検証の結論）なので、12本を先に回した。

| 層 | 結果 |
|---|---|
| ASCII／ひらがな／カタカナ／JIS第1水準の常用漢字 | **9/9 破綻ゼロ** |
| JIS第2水準（鵺鰰） | 偏だけ合って**旁が別の実在漢字** |
| 異体字（髙﨑・0208外） | **通常字体に黙って置換** |
| 国字（峠辻） | 辻○ 峠× — 同じ国字で割れたので「中国語に在るか」は効かない。稀で複雑な字ほど崩れる |
| カメラが動く／字種を本文で明示／中国語簡体 vs 日本語 | **すべて差なし** |

**設計に効いた3点:**
1. **壊れ方が「読めない」ではなく「別の実在漢字」。** 日本語が読めない人が確認すると見逃す。だから**入力時に水準を機械判定して先に警告**する（`textcheck.py`。目視分類は 2/8 外したので Shift_JIS のバイト列で判定）
2. **場所欄の外に独立した `TEXT:` 行を出す。** 寄りの画角で場所欄が「背景ヒント（画角には入らない）」に降格されても巻き込まれない。
   降格の断言を弱める案（「このうち画角に入らないものは〜」）は**採らなかった** — 仕分けを LLM に戻すことになり、`fix` が効いた理由（入力を変える、LLM に判断させない）と矛盾する。入力を分ける方が一貫する
3. **`system_h3` に足す文は短く、置く場所を選ぶ。** v12 の回帰疑い（結局ノイズだったが）で「足した文の配置」が疑われた。41語・ON-SCREEN TEXT 節・CAMERA の2文から離す

**検査は `h3lint` の D13（error）。** 期待文字が直線ダブルクォートつきで本文に無ければ修復ループが回る。
`check(..., expect_text=None)` の省略可能引数なので、bench2 / h3gen の数字は動かない。

**回帰測定の判定を見直した。** 同じ版でもシードを変えると一発率が **±3 ぶれる**（v11: 25→22）。
v12 のとき最初の28本で「回帰あり」と報告したが、シード5〜8で再現せず、**基準自身のブレより小さかった**。
`bench_regression.py` は -3 以内を判定しない。1回で決めず、シードを変えてもう一度流す。

### LLM を載せる前に ComfyUI を空ける（2026-08-24）

**症状**: ComfyUI が VRAM を抱えた状態で LM Studio を載せると、GPU に一部しか載らず残りが CPU に置かれる。
`lms ps` の SIZE は満額を表示するので気づけない。プロンプト生成が **45秒 → 218秒**（実測1回。§11a の事件②。ctx 固定の事件①=215秒とは別件）。
**あとから ComfyUI を空けても CPU 側の重みは GPU に移らない**（載せ直しが要る）。生成 → プロンプト生成の往復で踏む。

**対処** `gpu.prepare_for_llm()`。順番が設計そのもの:

| 状況 | する事 |
|---|---|
| 既に正しく載っていて**計測済み** | **何もしない**。無用な解放は次の生成の UNET 読み直し（15〜250秒）を招く |
| 載せ直しが要る | **先に ComfyUI を降ろす** → 載せる → 増分を検算 |
| 検算で足りない | 一度だけ載せ直す |

**検算に使えなかったもの（実測で判明）:**
- **合計 VRAM との比較** — ComfyUI も VRAM を持つので「モデルより多いから載りきっている」と誤判定する。
  ComfyUI 5.2GB 保持中に `--gpu 0.4` で半載りさせたら、合計 23.3GB > モデル 16.3GB で resident=True と出た
- **`/system_stats` の `vram_free`** — ComfyUI 自身の torch 会計で、デバイス全体の空きではない。
  20.5GB 空きと報告する裏で nvidia-smi の実際の空きは 4.1GB だった

**使えるのはロード前後の増分だけ**。`prepare_for_llm` が自分で載せたときにだけ測り、載せていないときは「未計測」と正直に返す
（未計測を「良い」と扱うと半載りのまま素通りするため、`_already_good` は未計測を False にする）。

### 低 VRAM 環境への構え（2026-08-24）

**このアプリは 24GB で UNET（約20GB）と LLM（約17GB）を時分割する前提**の上に建っている。
同時に載せるわけではないので必要なのは「大きい方＋作業領域」だが、**VRAM 量そのものへの適応は無い**
（`config.gen` の既定値はすべて 24GB 実測から来た固定値で、GPU サイズで分岐しない）。

調査（`../メンテ/調査_低VRAM環境.md`）で分かった一番大きな欠落は、**「親切に止まる」経路が無かった**こと。
`preflight()` はノードと重みの実在は見るが VRAM 容量を見ておらず、足りない環境では
PyTorch の生トレースバックが出るか、step 1 が数分かかる形で黙って遅くなるかのどちらかだった。

対処 `comfy.vram_check()` — **落とさない・止めない。先に言うだけ。**

| 総 VRAM | 判定 | 出す言葉 |
|---|---|---|
| 23GB 以上（UNET 20 + 作業 3） | ok | 何も出さない |
| 20〜23GB | tight | 「作業領域がほとんど残らない。step 1 に数分かかる形で出る」 |
| 20GB 未満 | warn | 「生成が VRAM 不足で失敗する可能性が高い。解像度や量子化の調整が要る」 |

**断定しないのは実測が 24GB の1環境しか無いから。** 「動かない」と書ける根拠が無いので、
代わりに**何が起きるか**を書く。判定に使うのは `/system_stats` の総容量だけで、空き容量は使わない
（ComfyUI 自身の torch 会計でデバイス全体の空きを表さないことが実測で判明済み）。

### 検証の作法 — 数字だけで判断しない（2026-08-23 に同じ罠を3回踏んだ）

| 指標 | 何と交絡したか | どう分かったか |
|---|---|---|
| **mafd**（フレーム間差分＝動きの量） | **画角**。寄った画では被写体の小さな動きが画面の大部分を変える | 超クローズアップ・動き指定なしの A2-F01 が全61本中2位なのに判定は `static` |
| **漏れ**（fix で画角外の語が何個残るか） | **情報量**。ユーザー入力を捨てるほど良い点になる | `strip` が 0.17 で最良に見えたが、本文を読むと全ケースが「暖色の琥珀光＋ベージュの無地壁」に収束し、雨の夜も水銀灯の体育館も反転していた |
| **一発率の差 -2**（bench） | **シード間のブレ ±3** | 同じ版でシードを変えただけでベースラインが 25→22 |

共通点: **数字が動いた方向は正しいが、測っているものが違った。** 本文・画を読んで初めて分かった。
ルール: (1) 指標を決めたら「これが満点になる最悪の出力は何か」を先に考える（漏れなら「全部無視」が満点）
(2) 差を主張する前に、同条件の再測定でブレの幅を知る (3) 上位と下位を**1本ずつ読む**。集計だけで結論を書かない。(4) **語数の増減も同じ** — 増えた＝情報が豊かとは限らない。発明で埋めていることがある。
同じ整理はカメラ検証側にもある: `../カメラ検証/結論.md` §5（再現するときの注意）、`結果_LLM.md` 結論3。重複させずそちらを参照。

## 10. v1 に入れないもの（明示）

- 複数ショットの連結・連続性検証（`shots[]` の構造だけ用意しておく）
- 音声のノーマライズ（-45dB 問題は後段処理。v2）
- 別 LLM への切替 UI（主モデル固定。`project.json` で変えられるようにだけしておく）
- 2K アップスケール

---

## 11. 作る順番（各段階で動くものがある）

| 段 | 内容 | 動作確認 |
|---|---|---|
| 1 | `server.py` + 画面 + プロジェクト読み書き | **済（2026-08-23）** 2作品を登録、API 全通過 |
| 2 | ブリーフ → プロンプト（h3gen）→ リンター表示 | **済** 実機 LM Studio で一発合格。モデル選択に実測ラベル付き |
| 3 | プロンプト確認・編集・再検査・`プロンプト.txt` 書き出し | **済** アーカイブ同時書き出し・project.shots 追記まで。**ここまでで「プロンプト作成ツール」として使える** |
| 4 | GPU 切替 + ComfyUI 投入 + 進捗 WS | **済（2026-08-23）** プレビューは実測 **約7分**（3分は出なかった。§9a の動的 VRAM の罠を参照）。中止・再接続・過去ジョブ一覧まで |
| 5 | コンタクトシート・メタ・採用→アーカイブ | **済（2026-08-23）** `inspect.py`（ffprobe/音量/3×3/フレーム間差分）と `/api/shots/archive`。本番 1344×768 も通した（所要は §11a） |
| 6 | モードA の展開、VLM 下書き、カメラ選択肢 | 入力が楽になる |
| 6b | **切り抜き画面**（SAM3 検出 → 選択 → プレビュー → 保存） | **済（2026-08-23）** `h3studio/cut.py`。threshold スイープ／個別マスク／ローカル合成／`check_cut`。実測の表（0.35→34人…0.9→1人）を画面から再現できた |

**段3で一度止めて使ってもらう。** ここで UI の違和感を直してから生成系に進む。

---

## 11a. 実測した速度（プロンプト生成 1本・正規化 2026-08-24）

**⚠ 「遅くなった事件」は別々の2件で、基準も違う。** 以前は 設計書 と 引き継ぎ に別々の数字が散らばっていて、
同じ事件だと思って読むと基準を取り違える状態だった（LMSTADIO セッションの指摘で正規化）。

**基準（正常系）— ComfyUI の状態で3段ある:**

| 条件 | プロンプト生成 1本 |
|---|---|
| **A1** ComfyUI 停止・ctx 16384・MTP あり | 15〜16秒 |
| **A2** ComfyUI 起動中（VRAM 9.2GB 保持）・ctx 16384・MTP あり | **35秒** ← アプリの通常運用の基準 |
| 同・MTP なし | 46秒 |

**事件（異常系）— 原因が違う独立の2件:**

| 事件 | 原因 | 実測 | 対処 |
|---|---|---|---|
| **①ctx 固定**（2026-08-23 朝） | モデル既定設定に 98304 が固定 → LM Studio が 58624 に自動切り詰め → VRAM 23.3GB | **16秒 → 215秒**（基準 A1 比） | `ensure_loaded` が実効 ctx を検算・警告（§9a） |
| **②半載り**（2026-08-23 夜・実測1回） | ComfyUI が VRAM を抱えた状態でロード → GPU に一部だけ載り残りが CPU | **45秒 → 218秒** | `gpu.prepare_for_llm`（空けてから載せる・増分検算）。再現手順は `../LMStudio検証/手順_半載り218秒の再現.md` |

215 と 218 は近い数字だが**別の事件**。①は ctx が原因（ComfyUI 停止でも起きる）、②は載せる順番が原因（ctx 16384 でも起きる）。
なお ctx 98304 は GUI 事故ではなく **8/22 に LM Studio 単独運用前提で意図的に置かれた設定**（引き継ぎ §7【記録の訂正】）。

LM Studio の初回ロード 10〜27秒（`ensure_loaded` が載っていれば即返る）。

### 生成（段4・2026-08-23、アプリ経由・参照画像2枚・192f・1019語のプロンプト）

| 条件 | 内訳 | 合計 |
|---|---|---|
| プレビュー 608×352 1本目（コールド） | 参照VAE 1s + テキストエンコード(CPU) 208s + step1 185s + 5step 47s + デコード 30s | **8.1 分** |
| プレビュー（`/free`＋Purge 2箇所、現行設定） | テキストエンコード 147s + step1 188s + 5step 47s + デコード 11s | **6.8 分** |
| 本番 1344×768（同設定） | テキストエンコード 約250s + step1 107s + 5step 各101〜104s（free 3.9GB で安定）+ デコード 46s | **15.6 分** |

**⚠ 上の表は 2026-08-23 の数字で、いまの share より遅い。** 同じ「プレビュー・`/free`＋Purge」でも
**step1 が 188秒 → 9.9秒**に変わっている（Purge 直後に UNET を載せる順序が効いた形。§9a の「良い行」に一致）。
プロンプト長と参照画像の枚数も違うので単純比較はできないが、**6.8分は現状の基準として使わないこと。**

**⚠⚠ 下の 2026-08-25 の表も無効（測り直し中）。** GPU の電力リミットが **50%（225W / 450W）** に絞られた状態での測定だった。
**測定するときは必ず電力リミットを確認する**（`nvidia-smi --query-gpu=enforced.power.limit --format=csv`。**360W = 80% が本機の基準**）。

| 【無効・50%電力リミット】条件（2026-08-25・6本の平均・LMSTADIO セッション） | 内訳 | 合計 |
|---|---|---|
| プレビュー 608×352・192f・6step・`vram_mode: share` | テキストエンコード 約88s + step1 9.9s + 残り | **153.3秒**（3本、幅 152.4〜154.5） |
| 同・`vram_mode: auto` の2本目以降（UNET 常駐） | エンコードは実行キャッシュ + step1 24.6s + 残り | **90.8秒**（2本） |

全文と限定条件は `../LMStudio検証/結果_vram_mode実測.md`、運用上の損益分岐は `引き継ぎ.md` §7。

step 1 の UNET 読込がブレの源（2026-08-25 実測で 6〜494 秒）。**`--reserve-vram` は効かなかった**（§9a 追記3）。**何が速い本と遅い本を分けているかは分かっていない。**

## 12. 未決（推奨案で進める）

| 項目 | 推奨 | 代替 |
|---|---|---|
| ポート | 8765 | 任意 |
| プレビュー解像度 | 608×352（実測3分、崩れは少ない） | 864×480（安全圏上限、少し遅い） |
| seed の既定 | プロジェクト固定（比較しやすい） | 毎回ランダム |
| LM Studio が無いとき | エラー表示して止める | システムプロンプトをコピーできる画面を出す |
| アーカイブ .md | `system_h3_notes.txt` で自動生成して人が追記 | 手書き |
